```markdown
# 🌸 Единый деканат

Веб-приложение для управления учебным процессом в вузе.  
Студенты могут смотреть оценки и долги, следить за дедлайнами, преподаватели — выставлять оценки и устанавливать дедлайны, деканат — управлять долгами и формировать отчёты.

---

## О проекте

| Роль | Возможности |
|:--|:--|
| **Администратор** | Управление пользователями, ролями, факультетами |
| **Деканат** | Просмотр всех студентов, управление долгами, отчёты, приказы |
| **Преподаватель** | Ввод оценок, просмотр своих групп, пересдачи |
| **Студент** | Просмотр своих оценок, долгов, уведомлений |

В проекте использованы 2 локальных представления: Успеваемость и Дедлайны: преподаватели, студенты, уч процесс декомпозированы для простоты

---

## Технологии

| Компонент | Технология |
|:--|:--|
| **Backend** | Python 3.10+, Flask, SQLAlchemy |
| **Frontend** | HTML, CSS 
| **База данных** | SQLite (файл `instance/university.db`) |

---

## Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/Katena1906/DEANERYGG
cd deanery
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
```

### 3. Активировать окружение

**Windows (PowerShell):**
```bash
venv\Scripts\activate.bat
```

**Windows (CMD):**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

### 4. Установить зависимости

```bash
pip install -r requirements.txt
```

### 5. Запустить приложение

```bash
python app.py
```

Откройте браузер: **http://127.0.0.1:5000**

---

## Тестовые учётные данные
В тг
---

## 🗄️ Работа с базой данных

База данных находится в файле `instance/university.db`. Это SQLite-файл, который можно открыть с помощью:

### DB Browser for SQLite (рекомендуется) в VS Code нет работающих(поддерживаемых) расширений с этой бд, не пытайтесь

**Что это такое?**  
DB Browser for SQLite — это бесплатная программа с графическим интерфейсом, которая позволяет просматривать и редактировать SQLite-базы данных без написания SQL-запросов.

**Где скачать?**  
👉 [https://sqlitebrowser.org/dl/](https://sqlitebrowser.org/dl/)

**Как открыть базу данных:**
1. Установить DB Browser for SQLite
2. Открыть программу
3. Нажать **"Open Database"** фотка в тг
4. Выбрать файл `instance/university.db`
5. На вкладке **"Browse Data"** выбрать таблицу для просмотра

**Что можно делать:**
- Просматривать все таблицы и данные
- Редактировать ячейки
- Выполнять SQL-запросы на вкладке **"Execute SQL"**
- Экспортировать данные в CSV, JSON, Excel


---

## Для разработчиков

### Структура проекта

```
deanery/
├── app.py                          # Основной файл приложения
├── requirements.txt                # Зависимости
├── README.md                       # Документация
│
├── blueprints/                     # Маршруты приложения(html)
│   ├── __init__.py
│   ├── auth.py                     # Аутентификация (login, logout)
│   ├── dashboard.py                # Дашборд (редирект по ролям)
│   ├── student.py                  # Маршруты студента
│   ├── teacher.py                  # Маршруты преподавателя
│   ├── dean.py                     # Маршруты деканата
│   └── admin.py                    # Маршруты администратора
│
├── database/                       # Работа с базой данных
│   ├── __init__.py
│   ├── db.py                       # Инициализация SQLAlchemy
│   └── models.py                   # Модели данных (User, Student, Teacher, 
│                                   # Faculty, Group, Grade, AcademicDebt, 
│                                   # Retake, Event, Deadline, Notification)
│
├── decorators/                     # Декораторы
│   ├── __init__.py
│   ├── auth.py                     # login_required, role_required
│   └── permissions.py              # permission_required
│
├── services/                       # Бизнес-логика
│   ├── __init__.py
│   ├── auth_service.py             # Аутентификация (bcrypt, блокировка)
│   ├── authz_service.py            # Права доступа (ROLE_PERMISSIONS, ACTION_NAMES)(ACL)
│   └── data_service.py             # Работа с данными (статистика, группы, оценки)
│
├── templates/                      # HTML шаблоны
│   ├── base.html                   # Базовый шаблон
│   ├── login.html                  # Страница входа
│   ├── dashboard.html              # Дашборд (редирект)
│   │
│   ├── student/                    # Шаблоны студента
│   │   └── panel.html              # Панель студента
│   │   ...
│   │
│   ├── teacher/                    # Шаблоны преподавателя
│   │   └── panel.html              # Панель преподавателя
│   │   ...
│   │
│   ├── dean/                       # Шаблоны деканата
│   │   └── panel.html              # Панель деканата
│   │   ...
│   │
│   ├── admin/                      # Шаблоны администратора
│   │   ├── panel.html              # Панель администратора
│   │   ...
│   │   
│   │   
│   │   
│   │
│   └── errors/                     # Страницы ошибок
│       ├── 403.html                # Доступ запрещен
│       ├── 404.html                # Страница не найдена
│       └── 500.html                # Внутренняя ошибка
│
├── static/                         # Статические файлы
│   ├── style.css                   # Основные стили
│   └── sakura-bg.gif               # Фоновое видео с сакурой
│
└── instance/                       # База данных (создается автоматически)
    └── university.db                  # SQLite файл


### Полезные команды

| Команда | Что делает |
|:--|:--|
| `python app.py` | Запустить сервер |
| `pip freeze > requirements.txt` | Сохранить текущие зависимости |

### Как добавить нового пользователя вручную

```python
print("Только попробуйте сломать бд добавляя через питон! Юзайте СУБД!")
```

---

## 🤝 Работа в команде (О боже)(Git)

### Получить последние изменения

```bash
git pull
```

### Отправить свои изменения

```bash
git add .
git commit -m "Описание изменений"
git push
```

### Если возник конфликт

1. Открыть файл с конфликтом
2. Найти строки с `<<<<<<<` и `=======`
3. Оставить нужный код
4. Удалить маркеры конфликта
5. Выполнить:
   ```bash
   git add .
   git commit -m "Resolve merge conflict"
   git push
   ```

---
